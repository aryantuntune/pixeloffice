services:
  app:
    image: djdiptayan/pixeloffice:latest
    pull_policy: always
    container_name: pixeloffice-app
    init: true
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PORT: "2567"
    ports:
      - "2567:2567"
